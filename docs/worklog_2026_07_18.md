# Suki HRMS — Daily Worklog

**Date:** 18 July 2026
**Project:** Suki HRMS
**Developer:** Roshini
**Repo:** https://github.com/roshinisuki/Suki_hrms.git

---

## Objective

Continue building the Suki HRMS application across multiple feature branches: Employee Master CRUD, Auth Login flow, shared UI components, Master Setup CRUD, RBAC wiring, layout shell, and a KUN→Suki branding rename. Verify each feature end-to-end with hard checkpoints before pushing.

---

## Tasks Completed

### 1. Employee Master — Schema + CRUD (feature/employee-master-models)

**Commits:**
- `9692f89` — feat: employee master schema + CRUD (list/create/edit/view/soft-delete)
- `6ade792` — docs: add DECISIONS_PENDING_REVIEW.md

**What was done:**
- Added Employee root model + sub-entity Prisma models (JobInfo, SalaryStructure, Documents, etc.)
- Implemented full CRUD: list, create, edit, view, soft-delete
- FK relations to existing Org structure (Department, Designation, EmployeeType, etc.)
- Soft-delete pattern (isActive + deletedAt) consistent with all other tables
- Self-referential FK for reporting manager
- Versioned tables for JobInfo and SalaryStructure (effectiveFrom/effectiveTo)
- Zod validation for all API requests
- `DECISIONS_PENDING_REVIEW.md` created to log all UX/schema design decisions

**Testing:**
- Verified FK constraints with real API calls (POST /api/employees)
- Confirmed soft-delete works (isActive=false, deletedAt set)
- TypeScript: zero errors

---

### 2. Auth Login Flow (feature/auth-login)

**Commits:**
- `93a86c5` — feat: real login flow with User table, bcrypt, httpOnly cookie
- `ebd87be` — feat: home page redirects to /login if no auth cookie
- `7e6f22d` — feat: redesign login page with grayscale minimalist UI

**What was done:**
- Added `User` model to `prisma/schema.prisma` (id, email, passwordHash, roleId FK→Role, isActive, deletedAt)
- Added `users` back-relation on `Role` model
- Rewrote `POST /api/auth/login` — validates credentials against User table via bcrypt, issues JWT, sets `hrms-token` httpOnly cookie (24h expiry)
- Created `POST /api/auth/logout` — clears cookie
- Created `POST /api/auth/seed-user` — upserts admin role + admin user for testing
- Created `/login` page — initially basic, then redesigned to grayscale minimalist UI
- Modified home page (`/`) — server-side cookie check, redirects to `/login` if no token

**Login page redesign (7e6f22d):**
- Grayscale design tokens added to `globals.css`: `--text-primary`, `--text-secondary`, `--border`, `--border-strong`, `--surface-1`, `--surface-2` (with dark mode variants)
- Centered card on plain page, no sidebar/header chrome
- "Suki HRMS" wordmark + "Sign in" subtitle
- Email + password fields with right-aligned "Forgot?" links (placeholder only, no flow)
- "Stay logged in" checkbox
- Full-width solid dark button (var(--text-primary) fill)
- Zod validation (email format + password required) with inline field errors
- Error message: bold text + border (grayscale, no red banner)
- All colors via CSS variables, zero hardcoded accent colors

**Testing (CHECKPOINT 2 & 3):**
- Correct credentials: 200 + cookie set + user info returned
- Wrong password: 401 "Invalid credentials"
- Non-existent email: 401 "Invalid credentials"
- Missing fields: 400 "Email and password are required"
- Logout: 200 + cookie cleared
- TypeScript: zero errors

---

### 3. KUN → Suki Branding Rename (chore/rename-kun-to-suki)

**Commits:**
- `e1f48a1` — Merge branch 'feature/rbac-master-wiring' into chore/rename-kun-to-suki
- `f1fd364` — fix: keep auth-login version of login routes (real bcrypt, not skeleton)
- `4b25e65` — chore: rename KUN → Suki across codebase

**What was done:**
- Scanned entire repo for KUN/kun/Kun references (case variants)
- Found 15 matches across 7 files — all UI strings, seed data, or comments
- Zero code identifiers, zero file names, zero DB table/column names, zero compliance/statutory items
- Replaced all: `KUN HRMS` → `Suki HRMS`, `admin@kun.hrms` → `admin@suki.hrms`
- Files changed: Sidebar.tsx, page.tsx, login/page.tsx, layout.tsx, seed-user/route.ts, schema.prisma, slab_overlap_triggers.sql, worklog_2026_07_17.md, feature-prisma-base-worklog.md
- 9 files, 18 insertions, 18 deletions — pure text replacement, no logic changes

**Merge & revert:**
- Merged `chore/rename-kun-to-suki` into `feature/auth-login` (`b136877`)
- Conflict in globals.css and login/page.tsx — resolved by keeping auth-login versions
- Later reverted the merge (`5fdb0ae`) to keep branches separate per user request

---

### 4. Shared CRUD Components (feature/shared-crud-components)

**Commit:**
- `ad1f2d4` — feat: shared CRUD UI components (DataTable, FormModal, ConfirmDialog, Field)

**What was done:**
- Built reusable UI components for master CRUD pages
- DataTable: sortable, paginated table with row actions
- FormModal: modal dialog for create/edit forms
- ConfirmDialog: confirmation dialog for delete actions
- Field: labeled input wrapper with validation error display

---

### 5. Layout Shell (feature/layout-shell)

**Commit:**
- `569ede8` — feat(layout-shell): add responsive app shell with theme system

**What was done:**
- Responsive AppShell with Sidebar + Topbar
- Theme system with CSS variables (light/dark mode)
- Mobile-responsive sidebar with overlay
- Sidebar brand + footer + placeholder navigation

---

### 6. Master Setup CRUD (feature/master-setup-crud)

**Commits:**
- `a5b32d5` — feat: shared CRUD components + Department master (Checkpoint 1, Batch 1)
- `387833d` — feat: remaining 9 Pattern A master tables (Checkpoint 1, Batch 2)
- `18792cd` — feat: Patterns B-F master tables — 9 tables with CRUD (Checkpoint 2, Batch 3)

**What was done:**
- 19 master tables with full CRUD (API + UI):
  - Pattern A (simple name/description): Units, Grades, Levels, Categories, EmployeeTypes, Designations, Departments, SubDepartments, LeaveMasters, LoanTypes
  - Pattern B-F (slab/rate tables with date ranges): TDSSlabs, ProfessionalTaxSlabs, EsiRates, PfRates, ShiftMasters, ShiftPlans, OtPlans, DropdownMaster, AssetMasters
- Zod validation schemas for all master types
- Reused shared CRUD components (DataTable, FormModal, ConfirmDialog, Field)

---

### 7. RBAC Master Wiring (feature/rbac-master-wiring)

**Commits:**
- `be18db3` — merge: bring in prisma-base, layout-shell, shared-crud-components, and master-setup-crud
- `bfee025` — feat: wire RBAC permission checks to all 19 master setup routes

**What was done:**
- Created `src/lib/rbac-masters.ts` — maps master API routes to permission groups (org, statutory, shift, dropdown, definition)
- Wired permission checks into all 19 master route handlers
- Created `src/app/api/auth/seed/route.ts` — seeds all RBAC permissions for master groups
- Updated middleware to enforce permission checks on `/api/masters/*` routes

---

## Branch State at End of Day

| Branch | Latest Commit | Status |
|--------|--------------|--------|
| `dev` | `ec94149` — README only | Base branch, no app code yet |
| `feature/auth-login` | `5fdb0ae` — revert rename merge | Login flow + employee master + redesigned login UI |
| `chore/rename-kun-to-suki` | `4b25e65` — KUN→Suki rename | Complete, ready to merge into any branch |
| `feature/employee-master-models` | `6ade792` — DECISIONS doc | Employee schema + CRUD |
| `feature/rbac-master-wiring` | `bfee025` — RBAC on 19 routes | Full master CRUD + RBAC + layout + shared components |
| `feature/master-setup-crud` | `18792cd` — 19 master tables | Master CRUD pages + API |
| `feature/shared-crud-components` | `ad1f2d4` — UI components | DataTable, FormModal, ConfirmDialog, Field |
| `feature/layout-shell` | `569ede8` — app shell | Responsive layout + theme system |
| `feature/prisma-base` | `4061fc3` — schema + RBAC skeleton | Base Prisma schema + JWT auth |

---

## Key Decisions Made

1. **Login page design:** Grayscale minimalist — no accent colors, uses CSS variables only, bold text + border for errors (not red banners)
2. **KUN→Suki rename:** Pure text replacement, no DB structure changes, no compliance/statutory items touched
3. **Branch strategy:** Reverted rename merge from auth-login to keep branches independent; rename will be applied per-branch as needed
4. **Seed user credentials:** `admin@suki.hrms` / `admin123` (on rename branch), `admin@kun.hrms` / `admin123` (on auth-login branch pre-rename)
5. **Design tokens:** Added to `globals.css` on auth-login branch — `--text-primary`, `--text-secondary`, `--border`, `--border-strong`, `--surface-1`, `--surface-2` with dark mode variants

---

## Pending / Next Steps

- Merge feature branches into `dev` in the right order (prisma-base → layout-shell → shared-crud-components → master-setup-crud → employee-master-models → rbac-master-wiring → auth-login → rename)
- Apply KUN→Suki rename to all branches that still have KUN references
- Build forgot-password flow (currently just UI placeholder links)
- Wire auth checks onto Employee Master routes (separate from RBAC master wiring)
- Begin Phase 2 modules (Attendance, Payroll, etc.)
